[Unit]
Description=llama-server (profile: full-128k) - primary daily-driver config
Documentation=file:%h/llama-server/README.md
# Only one profile should run at a time; starting this one stops the others.
Conflicts=llama-server-full-256k.service llama-server-limited.service

[Service]
Type=simple
WorkingDirectory=%h/llama-server
# Tunables live outside the repo (installed by ../install.sh) so they
# survive `git pull` / reclones; llm-server.sh treats a missing file as
# "use built-in defaults" rather than an error, so this works even before
# install.sh has been run.
ExecStart=%h/llama-server/scripts/llm-server.sh --tunables-file %h/.config/llama-server/full-128k.env
Restart=on-failure
RestartSec=5
# Model load (22GB, --no-mmap) plus KV cache allocation can take a while.
TimeoutStartSec=300
# Safety net for a desktop box also used for browsing: cap this cgroup so a
# runaway (this model's context-checkpoint bug or anything else) gets killed
# and restarted by systemd instead of taking down the whole session via the
# system-wide OOM killer. This profile's baseline RAM hasn't been directly
# measured (fewer ncmoe layers stay on CPU here than limited's, so likely
# somewhat lower) - cache-ram=8192 (see README's "Memory stability"
# section) means up to ~8G more on top of baseline in the worst case.
# Raised from 26G/28G after observing `limited` (same cache-ram=8192 at the
# time) repeatedly hit its MemoryHigh ceiling in real use (memory.events
# "high" counter in the thousands) and get pushed into swap purely from
# throttling, not an actual leak - these ceilings give real headroom above
# that same worst case instead of penalizing normal cache usage. Watch
# `systemctl --user status llama-server-full-128k` peak memory and adjust.
MemoryHigh=28G
MemoryMax=30G
# Prefer the kernel kill *this* process over other apps in a system-wide OOM,
# since it's expendable (Restart=on-failure brings it back) and your desktop
# session isn't.
OOMScoreAdjust=500

[Install]
WantedBy=default.target
